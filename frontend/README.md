# QRit Frontend

Next.js 16 frontend for the QRit QR code platform.

## Dev

```bash
npm install
npm run dev
```

Requires the backend running on port 8084. Set `NEXT_PUBLIC_API_URL` in `.env.local`.
